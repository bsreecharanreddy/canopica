# IES — Implementation Status

**Read this file first.** It is the authoritative record of where the
implementation stands against the full plan. It is updated and committed
*in the same commit* as the work it describes, so it never drifts and
always survives a closed session, a new machine, or a fresh pair of eyes.

Last updated: **2026-08-22**

---

## Current position

**Phase 1a complete, verified end-to-end. Phase 1b planned, not started.**

All 13 tasks of `docs/plans/2026-08-21-phase-1a-implementation-plan.md` are
done. The full walking skeleton — intake → DMN determination (persisted
trace) → hash-chained audit → dbt bronze/silver/gold → Metabase dashboard —
runs against real infrastructure end to end, proven automatically by
`data-platform/tests/test_end_to_end.py` (`make e2e`, and CI's `e2e` job on
every push) and walkable by hand via `docs/demo.md`. Every item in the
plan's own "Phase 1a definition of done" checklist is satisfied — see
Task 13's verification-log row below for how each one was actually
checked, not just asserted.

Phase 1b's design decisions are recorded in
`docs/design/2026-08-22-phase-1b-hardening-design.md` (see the "Decisions
already made" table below), and its 10-task implementation plan is written:
`docs/plans/2026-08-22-phase-1b-implementation-plan.md`.

**Phase 1b, Task 1 (Keycloak) is done** — real OIDC login (two realms,
Authorization Code + PKCE) has replaced `HardcodedRoleFilter` end to end,
verified against a real Keycloak (Testcontainers in the Java suite, the
real `infra/docker-compose.yml` service manually) including a live-browser
click-through of both the citizen and worker login paths. See Task 1's
verification-log row below.

**Next action:** Awaiting explicit go-ahead to start Task 2 (row-level
authorization) of the Phase 1b implementation plan.

---

## Verification log

Every row records a full-suite run, not a partial one.

| Date | Scope | Result |
|---|---|---|
| 2026-08-22 | Phase 1b, Task 1 -- `./mvnw -pl portal -am test` (Testcontainers Keycloak), `make test`, `make lint`, then a real Docker Compose cycle: `make up`, `uv run pytest -m e2e`, `make seed` (500 households through the real citizen-authenticated intake path), a live Playwright browser session (citizen login -> real submission, worker login -> real case list -> real determination run -> sign-out), `make down` | All green. `make test`: Java 55 (43 portal, up from 41 -- net new `AuthorizationTest` cases; 12 rules-engine, unchanged), web 7, Python 19 non-e2e, no regressions. `make lint`: `ruff`/`mypy`/`tsc` all clean. `pytest -m e2e`: 5 passed (4 stack-smoke incl. the new Keycloak realm-descriptor check, plus the full intake-through-mart test, now authenticating with a real worker token). Real OIDC replaces `HardcodedRoleFilter`: two Keycloak realms (`identity/realm-export/ies-workers-realm.json`, `ies-citizens-realm.json`, hand-written and reviewable, not a raw `kc.sh export`), two Spring Security resource-server chains in `SecurityConfig` matched by request path (POST `/api/applications` -> citizens realm, everything else -> workers realm), `KeycloakWorkerSyncFilter` auto-provisions a `worker` row from the JWT's own claims on first login (new `V7__worker_keycloak_identity.sql` adds `worker.keycloak_subject`; `worker`/`case_assignment`/`worker.role`'s `WORKER`/`SUPERVISOR`/`ADMIN` values already existed, unused, since Phase 1a's Task 2). React gets a real login flow (`src/auth/`, `react-oidc-context`, Authorization Code + PKCE) replacing the old client-side role-header switch -- a realm-choice landing screen, then a real Keycloak redirect per realm. Three real, non-obvious bugs found by actually running this against real infrastructure, not just Testcontainers: (1) `JwtDecoders.fromIssuerLocation()` performs its OIDC-discovery HTTP call eagerly, at bean-creation time -- broke every *other* test class booting the same `@SpringBootTest` context without needing MockMvc/security at all (schema tests, policy-parameter tests), since they have no Keycloak container and Spring context startup failed on an unrelated dependency; fixed with a decoder that defers construction until first `decode()` call, so a test that never makes an authenticated request never pays for it. (2) Genuinely the more consequential one: **against the real Compose stack**, every request failed 401 ("Another algorithm expected, or no matching key(s) found") -- Keycloak stamps a token's `iss` claim as whichever address the *token-requesting* caller used to reach it, and every real caller (browser, the e2e test, `make seed`'s loader) runs on the host at `localhost:8081`, while portal-api's own `IES_KEYCLOAK_*_ISSUER_URI` env vars pointed at the container-network `keycloak:8080` -- a real mismatch, not a flake, since `fromIssuerLocation()` uses one address for both discovery *and* issuer validation. Fixed by splitting `ies.keycloak.{realm}-issuer-uri` (a pure comparison string, stays `localhost:8081` in both local dev and Compose) from a new `ies.keycloak.{realm}-jwks-uri` (a real network address, overridden to `keycloak:8080` only in `infra/docker-compose.yml`) via `NimbusJwtDecoder.withJwkSetUri(...)` + `JwtValidators.createDefaultWithIssuer(...)` instead of the one-address convenience method -- caught only because Task 1's own plan called for verifying against the real Compose service, not just Testcontainers. (3) Found live-browser-testing sign-out (an interaction no automated test exercised): clicking "Sign out" silently re-triggered a second Keycloak round-trip before returning to the realm-choice screen, since `removeUser()` resolves asynchronously and the app's own "not authenticated, redirect" effect fired again in that window -- invisible to the user only because Keycloak's SSO session was still alive, so it round-tripped silently rather than showing a login form, leaving a stale `?code=...&state=...` in the URL. Fixed with a mount-scoped `useRef` guard so auto-redirect fires once per login attempt, not on every render where `isAuthenticated` happens to be `false`; re-verified via the same live browser flow, clean the second time. `make seed`'s `ies_data.synthetic.loader.post_households` and `test_end_to_end.py` both updated to fetch a real token from the `test-customer`/`test-worker` confidential clients (Direct Access Grant) instead of sending `X-IES-Role`, the same "hit the real thing" standard as everything else these tests already hold to. Architecture diagram checked (checkpoint skill step 4): no change needed -- the README's diagram already shows "Portal" as one box; Keycloak lives inside that tier, and this task didn't add a new top-level component or data flow at that diagram's level of abstraction (the roadmap doc's own, more detailed §3.1 diagram already depicted Keycloak from the design phase). `README.md`'s "Honest limitations" bullet on identity corrected (no longer describes removed behavior); `CLAUDE.md`'s "Current status" line updated -- both minimal, task-scoped edits, not the phase-wrap-up rewrite Task 13's own pattern used, since only Task 1 of 10 is done. |
| 2026-08-22 | Task 13 -- full Step 3 recipe from a clean stack: `make down && make up && make seed && make pipeline`, `make test && make lint && make e2e`, `TESTCONTAINERS_RYUK_DISABLED=true uv run pytest -m integration` (data-platform), `make down` | All green -- Phase 1a's definition-of-done checklist is now fully satisfied, not just Task 13's own new test. `make test`: Java 53 (41 portal + 12 rules-engine, unchanged from Task 12), web 7, Python 19 (`make test`'s own non-e2e scope, unchanged from Task 11/12) -- no regressions. `make lint`: `ruff`/`mypy --strict`/`tsc` all clean. `make e2e`: 4 passed (the 3 pre-existing stack-smoke checks plus the new end-to-end test). `pytest -m integration`: 7 passed, with the pre-existing, already-documented `TESTCONTAINERS_RYUK_DISABLED=true` local workaround (the plan's own literal Step 3 command needs this env var on this machine, same Docker-Desktop-for-macOS Ryuk quirk `make test` already works around -- not a new issue, not needed in CI). New `data-platform/tests/test_end_to_end.py` -- one `@pytest.mark.e2e` test, no mocks, against the real `make up` stack: submits a household through the real `POST /api/applications`, runs a real determination through `POST /api/program-requests/{id}/determinations` as a worker, reads its persisted trace via `GET /api/determinations/{id}/trace`, calls the real `verify_chain()` against the live operational database, rebuilds bronze/silver/gold from that same live database via a real `dbt build`, and asserts the gold mart's `total_benefit_amount` for that row equals `Decimal` of the exact `benefitAmount` the determination endpoint returned -- the assertion that matters, per the plan: the dashboard's number is the rules engine's own number, not a re-derivation. Two real, non-obvious adaptations the plan's own illustrative test snippet needed, found by actually running it rather than trusting the sketch: (1) the plan's hardcoded `as_of="2025-06-15"` is unreachable in this environment for two independent reasons -- FactAssembler (portal) only sees `household_member` rows whose `effective_from` (set to the real submission date by `IntakeService`) is `<= as_of`, so `as_of` must be on/after "today," and separately `SNAP-FY2025` closed on 2025-09-30, already before any "today" this environment will ever see again -- fixed by using `as_of = date.today()` and asserting `policyParameterVersion` against the full known set of seeded versions (`{"SNAP-FY2025", "SNAP-FY2026"}`) rather than one hardcoded label, so the test stays correct regardless of which fiscal year is actually in force when it runs. (2) A fixed `benefit_month` (even "today's" own month) made the test non-idempotent against a live, already-populated Postgres: running it twice in a row against the same `make up` stack landed both determinations in the same `(benefit_month, outcome, reason_code, policy_parameter_version)` mart group, and the second run's `determination_count == 1` assertion failed with `2 == 1` -- a real bug, not a flake, reproduced deliberately. Fixed by giving each run its own random `benefit_month` (decoupled from `as_of`, which the real system doesn't tie it to either), then proved the fix by running the e2e test three times in a row against one live stack with zero teardown between runs, all green. New CI `e2e` job in `.github/workflows/ci.yml` (`needs: [java, python, dbt]`, brings the real Compose stack up via `docker compose up -d --build --wait`, runs `pytest -m e2e`, uploads Compose logs as a build artifact on every outcome, pass or fail) -- not yet verified on GitHub Actions itself as of this row (verified locally against the same commands CI runs; GitHub-side confirmation follows once pushed). New `docs/demo.md`, a five-minute click-by-click walkthrough -- written, then actually driven end to end in a real browser (Playwright) against the live stack rather than only written from reading the source, which caught two real inaccuracies before commit: a hand-computed guess at the demo household's benefit amount ($65) was wrong against the real DMN output ($170/month -- arithmetic error in the guess, not a product bug), and a guessed "evaluation order" for the trace panel's decision list didn't match the real order the trace endpoint actually returns (JSON key order, not a linear dependency chain) -- both rewritten to state only what was directly observed. `README.md`: "Status & roadmap" table's Phase 1a row flipped to Done; new "Honest limitations of what's built so far" section added (no real identity/authorization, no external verification interface, no orchestration, narrow rules-engine/bronze scope, no accessibility/observability work yet), pointing at the plan's own "Deferred out of Phase 1a, on purpose" list and the tech-stack tradeoffs doc. Architecture diagram checked per the checkpoint skill's step 4: no change needed -- audit, warehouse, and reporting were already depicted starting Tasks 6/10/11, and this task adds tests, docs, and CI, not a new component or data flow. `CLAUDE.md`: "Current status" and the Phase 1a entry in "Phase plan" flipped to "done, verified end-to-end," with the same what-was-verified-and-how specificity every other phase entry in that file already uses. |
| 2026-08-22 | Task 12 -- `make lint`, `make test` (full project), then a real Docker Compose cycle: `make down && make up` (fresh build), `uv run pytest -m e2e` (3 tests), `make seed`, `make pipeline` (run twice), `make down` | All green. `make lint`/`make test`: Java 41, web 7, Python 19, unchanged from Task 11, no regressions. Extended `infra/docker-compose.yml` (Task 11 left it with just `postgres`+`metabase`) with `portal-api` (multi-stage `portal/Dockerfile`: `maven:3.9-eclipse-temurin-17` reactor build -pl portal -am, since portal depends on the rules-engine module -> `eclipse-temurin:17-jre` runtime, `/actuator/health` healthcheck), `portal-web` (multi-stage `portal/web/Dockerfile`: `node:22-alpine` Vite build -> `nginx:alpine` serving the bundle, `nginx.conf` proxying `/api/` straight through to `portal-api:8080` unchanged since every portal-api endpoint already lives under `/api`), and the profile-gated one-shot `pipeline` service (`data-platform/Dockerfile`, a `uv`-based image running the new `ies_data.pipeline` module -- ingest -> `dbt build` -> `materialize_gold` -> `provision_metabase`, reusing Tasks 10/11's own functions/CLIs rather than duplicating logic). New `data-platform/tests/test_stack_smoke.py` (e2e-marked, hits all three published URLs/health endpoints for real). `Makefile`'s `seed`/`pipeline` targets (declared `.PHONY` since Task 1 but never defined until now) are real: `make seed` shells out to the existing `ies_data.synthetic.cli` generate+load commands against the running `portal-api`; `make pipeline` runs `docker compose --profile pipeline run --rm pipeline`. Real, non-obvious bug found and fixed: `portal-web`'s Dockerfile `HEALTHCHECK` (`wget http://localhost/`) failed with "connection refused" on every single check even though the server was completely fine -- BusyBox `wget` in the `nginx:alpine` image resolves `localhost` to `::1` first, but nginx only listens on `0.0.0.0:80` (no IPv6), confirmed by testing `127.0.0.1` directly inside the container (worked immediately) and via `netstat` (only an IPv4 listener existed); fixed both `portal-web` and `portal-api`'s Dockerfiles to pin `127.0.0.1` rather than rely on the container's `localhost` resolution order. Second real, non-obvious finding, confirmed as correct design rather than a bug: running the exact documented quickstart (`make up && make seed && make pipeline`) against a clean stack produced a `materialize_gold` result of `mart_determination_outcomes: 0 rows` -- traced to `ies_data.synthetic.loader.post_households` only ever calling `POST /api/applications` (the customer-facing intake endpoint, matching its Task 9 scope of generator+loader only), never a determination, which is a separate WORKER-role action (`POST /api/program-requests/{id}/determinations`) that nothing in `make seed` triggers by design. Verified this is the real, intended behavior (not a wiring bug) by manually running one real determination against a seeded program request and re-running `make pipeline`: the mart correctly picked up the new row (`1 row, DENIED, $0`, matching the determination's own real response), and `provision_metabase`'s idempotency held across the two `make pipeline` runs (same dashboard id both times, consistent with Task 11's own idempotency proof). Documented this honestly in the new README quickstart rather than implying a populated dashboard out of the box. Also fixed `portal/web/index.html`'s `<title>` from the Vite-scaffold-default "web" to "IES Portal" -- needed for real, not cosmetic: the plan's own smoke test asserts `"IES" in response.text` against the portal-web root, which is nginx serving static `index.html` before any client-side JS runs, so the literal `<title>` text is what that assertion actually reads. New `.dockerignore` at the repo root (portal-api's build context, since it needs the full Maven reactor including the sibling `rules-engine` module) plus one each for `data-platform/` and `portal/web/` (their own self-contained build contexts) -- the `portal/web` one matters for correctness, not just build speed: without it, `COPY . .` after `npm ci` would overwrite the freshly-installed, Linux-built `node_modules` with the host's own (macOS ARM) copy, corrupting native dependencies inside the image. README's architecture diagram checked (per this checkpoint skill's step 4): every component Task 12 wires together (Portal, Data Platform, Reporting) was already depicted from the design phase -- this task adds infrastructure around existing components, not a new one, so no diagram change was needed. |
| 2026-08-21 | Task 11 -- `uv run pytest -m integration tests/test_materialize.py` (2 tests), `uv run pytest tests/test_semantic_model.py` (2 tests), `uv run ruff check .`, `uv run mypy src tests` (strict), `make test`/`make lint` (full project) | All green. Python: +4 tests over Task 10 -- `materialize_gold()` copies a real dbt-built gold mart into a real serving Postgres database via DuckDB's postgres extension and the serving copy's `sum(total_benefit_amount)` matches the warehouse's own number exactly (not a re-aggregation), a second `materialize_gold()` run against the same serving database replaces the mart wholesale rather than erroring or doubling it, and the TMDL semantic model's declared columns/measures are checked against the gold mart's own `gold.yml` contract by parsing both, so the two can't silently drift apart. `make test`: Java 41, web 7, Python 19, all green, no regressions. New `ies_data.serving.materialize`/`cli` (DuckDB `attach ... (type postgres)`, wholesale rebuild per Phase 1a's own stated scope) and `ies_data.reporting.provision_metabase` (idempotent, finds-or-creates by name: the "IES Serving" database connection, the "Determinations by month and outcome" native question, and the "SNAP determinations" dashboard). `reporting/semantic-model/` (`ies.tmdl` + `tables/mart_determination_outcomes.tmdl`) declares the mart's 9 columns and three measures -- `Determinations` (`SUM(determination_count)`), `Eligible Rate` (`DIVIDE(SUM(eligible_count), SUM(determination_count))`), and `Average Benefit`, deliberately `DIVIDE(SUM(total_benefit_amount), SUM(determination_count))` rather than the more obvious `AVERAGE(average_benefit_amount)` -- the mart's grain is already one row per outcome group, so naively averaging those per-group averages would weight every group equally regardless of its actual determination count, understating/overstating the true mean whenever group sizes differ. `provision_metabase.py`'s request shapes (the `/api/setup` body, session-properties' `has-user-setup` field, the `PUT /api/dashboard/:id` `dashcards` shape) were verified against a live `metabase/metabase:latest` container (v0.63.14.2) during development rather than written from memory of Metabase's semi-documented API -- run twice against a fresh instance (once exercising the initial-setup path, once the login-and-find-existing path), confirming the database/card/dashboard counts by name stayed at exactly one each across both runs, and a Playwright screenshot of the resulting dashboard (real seeded numbers, not a mockup) is committed at `reporting/dashboard/snap-determinations-dashboard.png`. Created `infra/docker-compose.yml` (`postgres` + `metabase` services only) and `infra/postgres/init/01-databases.sql` ahead of Task 12's own turn, per Task 11's plan file list, which explicitly anticipates this ordering -- needed a real Metabase instance to verify `provision_metabase.py` against; `make up`/`make down` both run clean against this partial file, which Task 12 extends with `portal-api`/`portal-web`/`pipeline` rather than replacing. One real, non-obvious operational bug found running this for real, not just in the tests: `provision_metabase.py` never opens a database connection itself, it only sends `IES_SERVING_DSN`'s host to Metabase's own `/api/database` call -- so that host has to resolve for *Metabase's container*, not for whatever process runs the script. Invoking the script from the host machine with `IES_SERVING_DSN=...@localhost:5432/...` (correct for the host reaching Postgres through its published port) made Metabase itself fail to resolve `localhost` from inside its own container with a 400; fixed by pointing at the Compose service name (`...@postgres:5432/...`) instead, resolvable by any container on the same network. Not a problem in the real Task 12 pipeline, where `materialize_gold`/`provision_metabase` both run from inside the same one-shot `pipeline` container as `postgres`/`metabase` -- documented in `reporting/dashboard/README.md` so a future session doesn't rediscover it. README's architecture diagram checked (per this checkpoint skill's step 4): the gold-to-semantic-layer-to-Power-BI/Metabase flow and the "Postgres serving layer" tech-stack line were already depicted from the design phase, so no diagram change was needed here, same as Task 10's own finding. |
| 2026-08-21 | Task 10 -- `uv run pytest -m integration tests/test_extract.py tests/test_dbt_build.py` (3 tests), `uv run ruff check .`, `uv run mypy src tests` (strict), `make test`/`make lint` (full project) | All green. Python: +3 integration tests over Task 9 -- extraction lands every row from a real seeded Postgres instance with `_ingested_at`/`_source_table`/`_batch_id` added, a second extraction run appends rather than overwrites (Delta version 0 to 1, row count doubles), and a full `dbt build` against a fixture warehouse produces exactly one `mart_determination_outcomes` row and exits 0. `make test`: Java 41, web 7, Python 15, all green, no regressions. New `ies_data.ingestion.extract.extract_to_bronze` reads each operational table via `polars.read_database_uri` and lands it as an append-only Delta table under `warehouse/bronze/` with ingest metadata (roadmap §3.4.2's bronze contract: no reshaping, duplicates across batches expected and silver's job to resolve). New `dbt/ies_warehouse/` project: 5 silver models (`dim_person`, `dim_household`, `dim_policy_parameter_set` as SCD-2, `fct_program_request`, `fct_eligibility_determination`), each deduplicating bronze to the latest `_ingested_at` per natural key, and the `mart_determination_outcomes` gold mart Task 11 reads. Every silver column carries a `meta: {classification: PUBLIC\|SENSITIVE}` tag; gold is gated by a custom `no_pii_in_gold` generic test (a name-based regex over `information_schema.columns`, factored into a reusable `is_pii_column` macro) that fails the build if a gold model ever exposes a column shaped like a name/SSN/DOB/address/email/phone field. New dependencies: `connectorx` and `pyarrow` (both required by `polars.read_database_uri`/`.to_arrow()`), `dbt-core` + `dbt-duckdb` (the toolchain the design doc already named, installed for the first time here). Two design gaps the plan's own file list didn't spell out in full SQL, resolved and documented inline as model comments: `dim_household` deliberately drops `address_line1`/`address_line2`/`city` (keeps `county`/`state`/`zip_code`) as more identifying than any planned mart needs, following the same "classify here, not later" rule `dim_person` states for names/DOB; `fct_program_request` carries no `household_key` since `application` sits outside this task's deliberately narrow 7-table bronze source list (`person`, `household`, `household_member`, `program_request`, `eligibility_determination`, `policy_parameter_set`, `policy_parameter`) -- Phase 1b widens both, per the roadmap's own stated intent for this task. Three real, fixed discrepancies, not just the new tests: (1) `Settings.warehouse_root`'s existing default (`Path("data-platform/warehouse")`, unexercised by any task before this one) was wrong for how the code actually runs -- every entry point runs via `uv run` from inside `data-platform/` already (see the Makefile), so the old default resolved to a nonexistent `data-platform/data-platform/warehouse`; fixed to `Path("warehouse")`. (2) The plan's illustrative default paths for `profiles.yml`'s `IES_DUCKDB_PATH` and `sources.yml`'s `IES_WAREHOUSE_ROOT` had the same bug relative to dbt's actual invocation (`cd dbt/ies_warehouse && dbt build`, the plan's own documented command) -- fixed both to `../../warehouse/...`, then verified by literally reproducing that exact documented command against a throwaway seeded Postgres container (real Flyway migrations, a hand-seeded person/household/application/program_request/eligibility_determination/determination_trace chain) and confirming `Done. PASS=68 WARN=0 ERROR=0`. (3) dbt 1.12.3 deprecates the un-nested generic-test argument shorthand (`relationships: {to: ..., field: ...}`, `accepted_values: {values: [...]}`) the plan's own snippets used; rewrote all 10 occurrences under the new `arguments:` key so the run stays at zero warnings. New `seeded_operational_dsn` fixture in `conftest.py` seeds a full real determination chain directly via SQL rather than through the API/DMN path -- extraction only needs real rows to land, not a real rules-engine run. Checked the README's architecture diagram (per the checkpoint skill's step 4): bronze/silver/gold and the dbt+DuckDB tech-stack line were already depicted from the design phase, so no diagram change was needed here. |
| 2026-08-21 | Task 9 -- `uv run pytest` (12 tests, 10 pass + 2 pre-existing Ryuk-affected, see Task 6), `uv run ruff check .`, `uv run mypy src tests` (strict), `make test`/`make lint` (full project) | All green. Python: +8 tests over Task 6 -- seeded reproducibility (same seed twice byte-for-byte equal, different seeds differ), the generated household-size distribution matches the committed PUMS marginal within 2pp at n=5,000, every generated household is internally consistent (exactly one SELF, at most one SPOUSE and one PARENT, income belongs to a real member, no earned income under age 16), and the generated payload's top-level keys match Task 7's real, deployed `IntakeRequest` shape. Real ACS PUMS 2024 1-Year microdata fetched live from the Census Bureau (Wyoming -- smallest file size, not a claim about deployment target, see the methodology doc's §5.1) via a new `fetch_pums.py`; computed marginals (household size; age by role; sex; disability by age band, computed but not yet sampled -- Task 7's intake API has no field for it; employment by age band; earned/unearned income deciles; tenure share; rent/mortgage/utility deciles) committed to `data/acs_pums_marginals.json` rather than fetched at test/build time. New `numpy` dependency (named explicitly in the approved plan for seeded sampling). `docs/design/synthetic-data-methodology.md` records the full provenance and six honest limitations, most materially: marginal (independent) sampling, not joint -- the same limitation the tech-stack doc's §4.9 already names for the Phase 4 fairness audit this data eventually feeds -- and PUMS's housing-unit-only sample structurally excludes homeless/institutionalized households, not a generator setting that could be flipped. One real, material deviation from the plan's own illustrative Task 9 test snippet, found and fixed before it could compound: the plan's `test_generated_payload_validates_against_the_intake_contract` asserted a payload shape (`{"county","members","incomes","expenses","livingArrangement"}`, top-level incomes/expenses arrays plus a nested `livingArrangement` object) that predates Task 7's actual implementation and doesn't match it -- Task 7's real `IntakeRequest` nests `incomes`/`expenses` *inside* each member and has no `livingArrangement` object, just flat `arrangementType`/`paysUtilitiesSeparately` fields. Rewrote the test to assert against the real, deployed contract instead of the plan's stale illustration, exactly the drift this test exists to catch. |
| 2026-08-21 | Task 8 -- `npm run typecheck && npm test` (web, 4 files/7 tests), `make test` (Java 41, Python 4), `make lint` | All green. Web: +6 tests over Task 1's single app-shell smoke test -- intake submission shows the confirmation reference number and surfaces a field-level `role="alert"` on a rejected submission (both from the plan's own test spec, run verbatim), the worker case list renders an accessible table (`aria-label="Cases"`) with column headers and a per-row link, and the case-detail page shows a determination's outcome/benefit/reason code/policy-parameter-version, re-renders with a new determination on "Run determination" without losing the prior one (append-only history, proven at the UI layer the same way Task 7 proved it at the API layer), and its collapsible trace panel lists each DMN decision name/value in evaluation order once expanded. One React app with role-gated views via `react-router-dom` (new dependency) and a `RoleContext`/`X-IES-Role`-header switch standing in for login, mirroring `HardcodedRoleFilter` on the client -- Phase 1b's real auth swap replaces this one context, not any page. `src/api/types.ts` hand-mirrors Task 7's Java DTOs; `src/api/client.ts` wraps `fetch`, attaches the role header, and turns a `400 {errors: [...]}` body into a typed `ApiValidationError`. Two real bugs found, not just the new tests: (1) `window.localStorage` is unavailable in this Node/jsdom/Vitest combination (`--localstorage-file` not provided) -- reading it eagerly at module-import time in `client.ts` threw synchronously and crashed the whole Vitest worker before any test in the file could run, cascading into three unrelated "worker timeout" failures that looked like nothing to do with localStorage until traced back; fixed by guarding every access in try/catch rather than a bare `typeof window` check, which doesn't protect against localStorage itself being missing/blocked. (2) `CaseDetailPage.test.tsx`'s own first draft asserted `screen.findByText(/eligible/i)`, which matched both the outcome text ("Eligible") and the reason code value ("ELIGIBLE") -- a real, sensible UI ambiguity, not a component bug; fixed by asserting the exact, case-sensitive string "Eligible" instead of a case-insensitive regex. |
| 2026-08-21 | Task 7 -- `make test` (Java 41 tests, portal now includes the API layer), `make lint` | All green. Java: +10 tests over Task 6 -- intake contract (submitting creates exactly one program_request + one APPLICATION_SUBMITTED audit event, rejects an empty `members` list with `errors[0].field == "members"`, rejects an income record whose effectiveTo precedes effectiveFrom), the worker case-view read surface (the caseload list carries head name/status/latest-determination summary, the case-detail endpoint returns determination history newest-first and appends CASE_VIEWED, the trace endpoint returns the same decision names the DMN model defines), and authorization (CUSTOMER gets 403 from a WORKER-only endpoint, a missing or unrecognized `X-IES-Role` header gets 401). New `spring-boot-starter-security` dependency; `HardcodedRoleFilter` sets the role header itself as the authenticated principal name (no other identity concept exists in Phase 1a), so `Authentication#getName()` is what every write endpoint records as `decided_by`/`actor_id` -- Phase 1b's Keycloak swap replaces one filter, not any controller. Also finally wired up `ies.timezone` (declared in application.yml since Task 1, unused until now) into a `Clock` bean, since intake is the first place "today" as a civil-calendar concept actually matters. Three real bugs found by the full-suite run, not just the new tests: (1) `@PathVariable UUID id` failed at request time with "Ensure the compiler uses the '-parameters' flag" -- this project's root pom never extended `spring-boot-starter-parent`, which is what normally sets that flag by default; fixed once, project-wide, via `maven.compiler.parameters=true` in the root pom rather than naming every path variable explicitly. (2) `HardcodedRoleFilter` NPE'd on a request with no `X-IES-Role` header at all (not just an unrecognized one) -- `Map.of(...).get(null)` throws rather than returning null, unlike `HashMap`; fixed with an explicit null guard before the lookup. (3) `IntakeControllerTest`'s own first draft asserted an absolute `count(*) from program_request` of 1, copied directly from the plan's example test -- passed in isolation but failed under `make test`'s full run (count was 10) because `AbstractPostgresTest`'s Postgres container is a JVM-wide singleton shared with every other test class's fixtures; fixed to scope the assertion to the specific `programRequestId` the test's own submission returned, the same "find by id, not by count/position" pattern `WorkerCaseControllerTest` used from the start for the same reason. |
| 2026-08-21 | Task 1 — `make test`, `make lint` (Java `./mvnw verify`, web `npm test`/`npm run typecheck`, Python `uv run pytest`/`ruff`/`mypy --strict`) | All green. Java: 1 test (context loads). Web: 1 test (app shell renders). Python: 2 tests (Settings config). No integration/e2e tests yet — no database, no Compose stack exists until Task 2 onward. |
| 2026-08-21 | Task 2 — `make test`, `make lint` (portal now runs 9 Testcontainers tests against a real Postgres 16 instance) | All green. Java: 9 tests — schema migration (V1/V2 apply, every Phase 1a table exists, every effective-dated table carries both date columns, household carries a mailing address), effective-dating/benefit-month constraint enforcement, and as-of query correctness for income_record. Web/Python unchanged from Task 1. Domain scope grew mid-task: `household` gained a mailing address (address_line1/2, city, state, zip_code) after clarifying that `household` is IES's case record — one HOH, many members via household_member, one or more applications over the case's life (including renewals) — and a case needs an address, which nothing in the schema carried yet. |
| 2026-08-21 | Task 3 — `make test`, `make lint` (portal now runs 17 Testcontainers tests) | All green. Java: +8 tests over Task 2 — as-of parameter resolution across the FY2025/FY2026 boundary (exact, not off-by-one), size-scoped vs. scalar parameter distinction, missing-size and missing-date rejection, and database-enforced immutability (UPDATE/DELETE on published parameter sets refused). FY2025 and FY2026 SNAP figures (max allotments, standard deductions, gross/net income limits, excess shelter cap, minimum benefit) verified directly against USDA FNS's published COLA memos before seeding — see docs/design/policy-parameter-provenance.md for citations; statutory figures (20% earned-income deduction, $35 medical threshold, 50% shelter share, 30% benefit reduction, 2-person minimum-benefit cutoff) cited to 7 U.S.C./7 CFR since they aren't in the COLA memos. One regression caught and fixed: Task 1's `IesPortalApplicationTest` excluded JPA/DataSource autoconfiguration (valid when written, before any database existed) and broke once Task 3 added a `@Component` requiring JPA repositories — converted to extend `AbstractPostgresTest` like every other test since Task 2, consistent with that class's own doc comment. |
| 2026-08-21 | Task 6 -- `make test` (Java 31 tests, Python 4 tests incl. 2 real Postgres-integration), `make lint` | All green. Java: +4 tests -- the chain's first row links from the zero-hash, every row links to its predecessor, UPDATE/DELETE both refused with "append-only", and every real determination appends exactly one DETERMINATION_MADE event carrying the policy parameter version in its payload. Python: a new `verify_chain()` CLI/library (`ies_data.audit.verify_chain`) plus two integration tests against a Postgres instance running the portal's *actual* Flyway migrations (via the `flyway/flyway` Docker image, not a copied-SQL approximation) -- proves an untampered 5-row chain verifies, and that a superuser directly disabling the trigger and editing a row is still caught (the verifier detects what no application-level control could have prevented). New CI job `audit-chain` runs this against GitHub Actions' own Docker daemon. One environment-specific issue found and fixed, not a code bug: Testcontainers-Python's Ryuk reaper container fails to start on this machine's Docker Desktop for macOS ("error while creating mount source path ...docker.sock: operation not supported") -- a known Docker-Desktop-macOS quirk affecting testcontainers-python specifically (the Java-side Testcontainers tests are unaffected). Worked around via `TESTCONTAINERS_RYUK_DISABLED=true`, scoped to the Makefile's Python test invocation only (not CI, which doesn't hit this on Linux runners) -- documented in `tests/conftest.py`'s module docstring so a future session recognizes it immediately rather than re-diagnosing. Also switched off the deprecated `testcontainers.postgres` import path to `testcontainers.community.postgres` while touching this file. |
| 2026-08-21 | Task 5 — `make test`, `make lint` (portal now runs 27 Testcontainers tests) | All green. Java: +10 tests over Task 4 -- fact assembly (as-of exclusion of not-yet-effective income, inclusion of later-effective income, elderly-by-age-60 and disability-record detection, SSI-driven categorical eligibility), determination persistence (determination + trace both written, append-only enforced, a denial stores a zero benefit and the correct reason code), and the exact property CLAUDE.md's testing policy names for the rules engine: an old determination re-run against its own stored parameter-set version reproduces its original answer even after both the household's income and the fiscal year have since moved on, plus a determination made on/after October 1 picks up the new fiscal year. All passed on the first real run -- no logic bugs, only two known Jackson/Hibernate wiring points handled up front: `@JdbcTypeCode(SqlTypes.JSON)` on the two jsonb-backed trace columns (plain String mapping fails Hibernate's ddl-auto=validate against a jsonb column), and SnapPolicyParameters resolution split into a shared `buildFromSet` helper so reproduction can pin to a stored parameter_set_id instead of re-resolving by "today's" date. |
| 2026-08-21 | Task 4 — `./mvnw -pl rules-engine test` (rules-engine module) | All green: 12 tests, 0 failures. 2 DMN model sanity tests (compiles clean, every named decision appears in the trace) + 9 table-driven SNAP scenarios (gross/net income pass/fail, each deduction in order, categorical eligibility, elderly/disabled uncapped shelter deduction, minimum-benefit floor, "computes to zero is a denial not a $0 award") + 1 as-of-date-correctness test (same facts, two parameter versions, different benefits -- proves parameters are injected, not baked in). All 12 passed on the first real run after two build-time fixes (Class-vs-ClassLoader argument to kie-dmn's classpath loader; a missing JUnit import) and one XML-syntax fix (an illegal double-hyphen inside an XML comment, unrelated to DMN semantics) -- no arithmetic mismatches, meaning the hand-computed expected values and the fifteen-decision DMN model agreed on every scenario. `make test`/`make lint` (full project) also green. |

---

## Phase 1a — Walking skeleton

The thinnest path that touches every layer and produces a real, correct,
auditable determination. Demoable on completion.

Task breakdown is the definitive one from
`docs/plans/2026-08-21-phase-1a-implementation-plan.md`; read that plan for
each task's files, interfaces, and test steps.

| # | Task | Status |
|---|---|---|
| 1 | Repo scaffolding, Maven/uv/Vite toolchains, CI skeleton | Done — 6af08cf |
| 2 | Operational schema, effective-dated (Flyway + Testcontainers) | Done |
| 3 | `policy_parameter_set` — effective-dated SNAP parameters + as-of resolver | Done |
| 4 | DMN decision tables on Drools/KIE, table-driven scenarios | Done |
| 5 | Determination service — persisted determination + DMN trace | Done |
| 6 | Hash-chained audit log + CI chain-verification job | Done |
| 7 | Portal API — intake + worker case view (roles hardcoded) | Done |
| 8 | React UI — intake form, case list, trace panel | Done |
| 9 | Synthetic applicant generator (ACS PUMS–driven) + loader | Done |
| 10 | Ingestion to Delta bronze + dbt silver/gold with tests | Done |
| 11 | Reporting — serving layer, Metabase, TMDL semantic model | Done |
| 12 | Docker Compose: full stack runs with one command | Done |
| 13 | End-to-end test (intake → determination → audit → warehouse → mart) + wrap-up | Done |

## Phase 1b — Hardening

| # | Task | Status |
|---|---|---|
| 1 | Keycloak — citizen + worker realms | Done |
| 2 | Row-level authorization — caseload scoping, sensitive-case flagging, `mart_access_review` | Not started |
| 3 | Mock external verification interface, with FTI-style safeguards applied | Not started |
| 4 | Airflow orchestration | Not started |
| 5 | Full medallion coverage of design §3.4.2's tables | Not started |
| 6 | Reporting widened — processing time vs. SNAP 30-day / 7-day standards | Not started |
| 7 | Governance completed — tokenization, column classification, `compliance-mapping.md` | Not started |
| 8 | Accessibility (Section 508/WCAG) | Not started |
| 9 | Observability (OpenTelemetry across API + pipeline) | Not started |
| 10 | Reference Terraform for Azure (not deployed) | Not started |

## Phases 2–5

Not started. Scope is defined in
`docs/design/2026-08-21-full-system-and-phased-roadmap.md` §5; task-level
breakdowns get written when each phase begins.

| Phase | Focus | Status |
|---|---|---|
| 2 | Policy Intelligence & Analytics AI — public hosted demo goes live here | Not started |
| 3 | Case Intake & Communication AI | Not started |
| 4 | Compliance & Integrity AI | Not started |
| 5 | Domain expansion (Medicaid/TANF) & real cloud deployment demos | Not started |

---

## Decisions already made (don't relitigate)

Recorded here so a fresh session doesn't reopen settled questions. Full
reasoning lives in the design docs.

| Decision | Choice | Where |
|---|---|---|
| DMN runtime | Drools/KIE, not Camunda | Roadmap §3.3 |
| Reporting | TMDL model-as-code + Power BI Service + Metabase container | Roadmap §3.3 |
| Audit log | Hash-chained, CI-verified | Roadmap §3.6 |
| Phase 1 shape | Split 1a / 1b | Roadmap §5 |
| Unit of eligibility | `PROGRAM_REQUEST`, not `APPLICATION` | Roadmap §3.4.1 |
| Determinations | Append-only; a change produces a new one | Roadmap §3.4.1 |
| Policy parameters | Effective-dated and immutable once published | Roadmap §3.5 |
| AI scope | All nine components stay in committed scope | Roadmap §5 |
| Cloud target | Azure Government by design; commercial Azure for any live demo | Roadmap §3.7 |
| Repo visibility | Private until Phase 1a ships | — |
| Java build tool | Maven (not Gradle) — dominant in the enterprise/government Java shops this repo targets; committed `./mvnw` wrapper is what CI runs | Phase 1a plan, "Versions pinned" |
| Python version | 3.12 via `uv`, not the system 3.14 — dbt-core support lags new releases | Phase 1a plan, "Versions pinned" |
| Bronze storage, Phase 1a | Local filesystem Delta tables; MinIO/S3 is a `storage_options` swap in Phase 1b | Phase 1a plan, Task 10 |
| Async messaging | **pgmq** (Postgres extension), not RabbitMQ/Kafka — document-intake jobs, correspondence dispatch, fraud-triage triggers. Portfolio-documented substitute, not an oversight. | Roadmap §3.3; tech-stack doc's Messaging tier + §4.11 |
| Ingestion pattern | Batch extract (Python job) for Phase 1a; CDC/Debezium documented as the production equivalent, not built — same treatment as pgmq, and for the same reason (needs a broker). | Roadmap §3.3; tech-stack doc's Data tier + §4.12 |
| Row-level auth model | Explicit `CASE_ASSIGNMENT` per household (not implicit by county); auto-claimed on first worker touch; `SUPERVISOR` can view any case, logged (not blocked) via a `CASE_VIEWED` payload flag; sensitive-case flag is flag-and-log, not sealing | Phase 1b design doc §2.1; tech-stack doc's Authorization row |
| Mock external verification shape | Synchronous REST, deterministic canned responses keyed by input hash, every request/response an audit-chain event, raw response gated by an active `CASE_ASSIGNMENT` | Phase 1b design doc §2.2; tech-stack doc's External verification row |
| PII tokenization | `pii_token` vault table (`pgcrypto`-encrypted), narrow-RLS'd; detokenization is a separate audited call, not a normal column read | Phase 1b design doc §2.3; tech-stack doc's Data tier + §4.15 |
| `mart_fairness_audit` timing | Deferred from Phase 1b to Phase 4, built alongside fraud-triage so it has both comparison axes from day one | Phase 1b design doc §2.4 |
| Observability stack | Jaeger (traces) + Prometheus/Grafana (metrics), self-hosted single-host — not a heavier collector/Tempo/Loki stack | Phase 1b design doc §2.5; tech-stack doc's Observability row |

## Open questions

| Question | Blocking? | Notes |
|---|---|---|
| Does the current free Databricks tier permit the Phase 5 demo? | No — Phase 5 | Community Edition was replaced; verify before the README promises it |
| Fabric's current Government-cloud availability | No — Phase 5 | Narrower than Synapse's; verify before stating specifics |
