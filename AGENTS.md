# AGENTS.md

This file exists for cross-tool compatibility with AI coding agents that
read `AGENTS.md` rather than `CLAUDE.md`. It is a pointer, not a second
copy — read [`CLAUDE.md`](CLAUDE.md) for the real content (what this
project is, the governing AI-drafts/deterministic-decides principle,
current status, and conventions). Keep them in sync by editing
`CLAUDE.md` and updating this pointer only if the pointed-to sections
change name, not by duplicating content here.
