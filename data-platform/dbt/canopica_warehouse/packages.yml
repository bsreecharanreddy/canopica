# Phase 4 Task 9 (design doc §2.7): adopt Elementary rather than a
# hand-rolled anomaly detector -- the tech-stack tradeoffs doc already
# names it as this project's stated production-equivalent gap. Pinned to
# the newest version that resolves cleanly against this project's real,
# installed dbt-core 1.12.3 (verified live via `dbt deps`, 2026-08-30;
# 0.19.4 also resolves but 0.25.1 is current). dbt_utils is Elementary's
# own transitive dependency, not something this project asked for
# directly.
packages:
  - package: elementary-data/elementary
    version: [">=0.25.0", "<0.30.0"]
