{
  "_note": "Real measurement (2026-08-26) against the 12-question CI-gate subset (run_eval.py's _CI_GATE_QUESTIONS, widened from 8 -- see run_eval.py's own module docstring point 6), on the Azure CI-runner VM (resized D2s_v7 -> D4s_v7 the same night for the memory headroom this run needed), judged by deepseek/deepseek-chat via OpenRouter's paid tier (Settings.openrouter_judge_model -- see config.py's own comment for why the free tier was dropped and why DeepSeek specifically), committed by hand per the plan's own requirement -- never auto-written by run_eval.py itself. citation_grounded_rate this run was 0.667 (8 of 12) -- not gated (see _METRIC_NAMES in run_eval.py), reported for visibility only; the 4 ungrounded questions are the categorical-eligibility 273.2(j) cluster's deterministic abstentions described in the module docstring's point 5. The three metrics below are judged only over the 8 grounded questions. A second run the same night, using anthropic/claude-haiku-4.5 as judge instead (live-verified working, tried before switching to DeepSeek -- see config.py's comment), independently regenerated and rejudged the same 12 questions and reported citation_grounded_rate 0.667 and faithfulness 0.750, with contextual_precision/recall again 1.000 -- the matching grounded-rate and precision/recall across two independent generations is retrieval/grounding behaving consistently; the faithfulness gap (0.750 vs 1.000 here) is judge-specific scoring on similar-but-not-identical generated text, not a generation regression. Recorded here as the committed number because deepseek/deepseek-chat is config.py's actual default, not because it's the only value observed -- _REGRESSION_MARGIN (0.05) is sized to absorb ordinary judge-score noise, not cross-judge disagreement of this size, so a future judge-model change should re-baseline rather than lean on the margin alone.",
  "faithfulness": 1.000,
  "contextual_precision": 1.000,
  "contextual_recall": 1.000
}
